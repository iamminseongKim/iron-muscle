### 🌟 최신 업데이트 핵심 요약

- **8개 언어 설정**: 한국어·영어·일본어·중국어 간체/번체·스페인어·프랑스어·독일어를 선택하고 저장할 수 있습니다. 최초 실행 시 기기 언어를 감지합니다.
- **AI 분석 Markdown 8개 언어 출력**: 제목·표·장비 중량 원칙·기간별/부위별 코칭 요청문을 번역했습니다. 사용자 메모와 제목은 원문 그대로 보존하고, 종목명은 한국어 또는 국제 영어 표준명을 사용합니다.
- **인증 이미지 8개 언어 출력**: 랜덤 추천 문구, 통계·세트 정보, 묶음 표시, 해부도 범례와 하단 설명을 번역했습니다. 긴 문구는 자동 줄바꿈 및 크기 조절로 처리합니다.
- **주요 화면 다국어 적용**: 메뉴, 운동 시작, 목표 부위, 컨디션, 내보내기 조작에 적용했습니다. 내보내기 이외 일부 고급 화면 설명은 아직 한국어가 남아 있습니다.
- **초기 로딩 코드 분리**: 3D 엔진·운동 탐색·기록·통계를 필요할 때 불러옵니다. 초기 JavaScript는 약 2.20MB에서 1.59MB로 약 28% 감소했습니다.
- **해부도 데이터 압축**: 배포 시 Meshopt와 gzip으로 압축하고 앱 내부에서 해제합니다. 원본 파일은 저장소에 보존하며, 매 빌드에서 668개 메시의 이름·정점·법선·삼각형 구성을 원본과 비교합니다.
- **불필요한 배포 파일 제외**: 소개용 스크린샷과 사용하지 않는 모델 매핑 파일을 앱 패키지에서 제외했습니다. 웹 산출물은 약 42MiB에서 26.5MiB로 줄었습니다. 이는 APK 파일 크기와는 다른 측정값입니다.
- **타이머 렌더링 분리**: 총 운동 시간만 바뀔 때 운동 입력 화면 전체를 다시 렌더링하지 않도록 했습니다.
- **종목 조회 개선**: 반복적인 전체 종목 검색을 ID 인덱스 조회로 바꿨습니다.

검증: npm test, npm run build, 8개 언어 변경·설정 유지·모바일 화면 및 압축 모델 데이터 무손실 비교, 315개 Markdown 언어·기간·부위 조합 검사. 실제 기기별 체감 성능은 기기 사양에 따라 다릅니다.

기존 운동 데이터 및 모든 사용자 기록은 100% 온전히 보존됩니다.
